Run the test suite against the deployed GAS web app.
Use the test-agent subagent to execute the full verification sequence.
The GAS URL should be provided as $ARGUMENTS or read from the last deploy output.
